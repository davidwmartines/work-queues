{
  "name": "work-queues-server",
  "version": "1.0.0",
  "description": "work queues app server",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.15.4"
  },
  "devDependencies": {
  }
}
